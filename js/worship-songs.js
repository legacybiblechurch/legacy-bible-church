/**
 * LBC Sunday Setlist
 * ──────────────────
 * SET BY HAND, ON PURPOSE — one known-good song while the prep workflow is
 * being reworked. The scheduled "Worship prep" Action is paused (see
 * .github/workflows/worship-prep.yml) so nothing overwrites this list.
 *
 * To go back to the automated setlist: re-enable the schedule in that
 * workflow file, and this file goes back to being generated from the
 * approved rows of the planner sheet.
 */

window.WORSHIP_SETLIST = [
  'my-soul-longs-for-the-lord',
];
